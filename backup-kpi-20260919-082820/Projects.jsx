import { useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import { FaPlus,FaProjectDiagram } from 'react-icons/fa';
import Card from '../components/ui/card'; import Title from '../components/ui/title'; import Button from '../components/ui/Button'; import {api} from '../services/api'; import {useAuth} from '../context/AuthContext';
export default function Projects(){const {profile}=useAuth();const canCreate=['SUPER_ADMIN','ADMIN','MANAGER'].includes(profile?.role);const [projects,setProjects]=useState([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{api.get('/projects').then(setProjects).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div className="mx-auto max-w-7xl pb-10"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">Organisation</p><Title as="h1" variant="page">Projets</Title><p className="text-sm text-muted">Suivez le périmètre, les sections et la progression validée.</p></div>{canCreate&&<Link to="/projets/nouveau"><Button><FaPlus/> Nouveau projet</Button></Link>}</div>{error&&<Card className="mt-6 border-red-200 bg-red-50 text-red-700">{error}</Card>}{loading?<div className="mt-6 grid gap-4 md:grid-cols-2"><div className="h-48 animate-pulse rounded-2xl bg-surface-2"/><div className="h-48 animate-pulse rounded-2xl bg-surface-2"/></div>:<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.map(p=><Link key={p.id} to={`/projets/${p.id}`}><Card className="h-full bg-white transition hover:-translate-y-0.5 hover:border-primary/30"><div className="flex items-start justify-between gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><FaProjectDiagram/></span><span className="rounded-full bg-surface-2 px-2 py-1 text-[10px] font-bold">{p.status}</span></div><h2 className="mt-4 text-base font-bold text-ink">{p.name}</h2><p className="mt-1 line-clamp-2 text-xs text-muted">{p.objective||p.description||'Aucun objectif renseigné.'}</p><div className="mt-5 flex items-end justify-between"><div><p className="text-2xl font-bold text-primary">{p.progress||0}%</p><p className="text-[11px] text-muted">progression validée</p></div><div className="text-right text-[11px] text-muted"><p>{p.validatedTaskCount||0}/{p.taskCount||0} tâches</p><p>{p.kpis?.overdue||0} retard</p></div></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-surface-2"><div className="h-full bg-primary" style={{width:`${p.progress||0}%`}}/></div></Card></Link>)}{!projects.length&&<Card className="md:col-span-2 xl:col-span-3 py-16 text-center text-sm text-muted">Aucun projet disponible.</Card>}</div>}</div>}
