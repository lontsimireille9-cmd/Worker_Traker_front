import {useEffect,useState} from 'react'; import {Link,useNavigate,useParams} from 'react-router-dom'; import {FaArrowLeft,FaCheck,FaChevronDown,FaChevronRight,FaClock,FaExclamationTriangle,FaPlus,FaUsers} from 'react-icons/fa'; import Card from '../components/ui/card'; import Title from '../components/ui/title'; import Button from '../components/ui/Button'; import {api} from '../services/api'; import {useAuth} from '../context/AuthContext';
const Progress=({value})=><div className="h-2 overflow-hidden rounded-full bg-surface-2"><div className="h-full bg-primary" style={{width:`${Math.min(100,value||0)}%`}}/></div>;
export default function ProjectDetails(){const {id}=useParams();const nav=useNavigate();const {profile}=useAuth();const canManage=['SUPER_ADMIN','ADMIN','MANAGER'].includes(profile?.role);const [data,setData]=useState(null);const [error,setError]=useState('');const [open,setOpen]=useState({});const [status,setStatus]=useState('');const [taskTitle,setTaskTitle]=useState('');const [saving,setSaving]=useState(false);const load=()=>api.get(`/projects/${id}`).then(setData).catch(e=>setError(e.message));useEffect(()=>{load()},[id]);async function changeStatus(v){setStatus(v);try{await api.patch(`/projects/${id}/status`,{status:v});await load()}catch(e){setError(e.message)}finally{setStatus('')}}async function addTask(sectionId){if(!taskTitle.trim())return;setSaving(true);try{await api.post('/tasks',{projectId:id,sectionId,title:taskTitle,assigneeId:profile.uid,estimatePoints:1});setTaskTitle('');await load()}catch(e){setError(e.message)}finally{setSaving(false)}}if(error&&!data)return <Card className="border-red-200 bg-red-50 text-red-700">{error}</Card>;if(!data)return <div className="mx-auto max-w-7xl animate-pulse"><div className="h-12 w-1/3 rounded bg-surface-2"/><div className="mt-6 h-96 rounded-2xl bg-surface-2"/></div>;return <div className="mx-auto max-w-7xl pb-10"><button onClick={()=>nav('/projets')} className="mb-4 inline-flex items-center gap-2 text-sm text-muted hover:text-ink"><FaArrowLeft/> Projets</button><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2"><span className="rounded-full bg-primary/10 px-2 py-1 text-[10px] font-bold text-primary">{data.status}</span><span className="text-xs text-muted">v{data.version||1}</span></div><Title as="h1" variant="page" className="mt-2">{data.name}</Title><p className="mt-1 max-w-3xl text-sm text-muted">{data.objective||data.description}</p></div>{canManage&&<select value={data.status} onChange={e=>changeStatus(e.target.value)} disabled={!!status} className="rounded-xl border border-line bg-white px-3 py-2 text-sm"><option>DRAFT</option><option>PLANNED</option><option>ACTIVE</option><option>PAUSED</option><option>COMPLETED</option><option>ARCHIVED</option><option>CANCELLED</option></select>}</div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card className="bg-white"><p className="text-xs text-muted">Progression</p><p className="mt-2 text-3xl font-bold text-primary">{data.progress||0}%</p><Progress value={data.progress}/></Card><Card className="bg-white"><p className="text-xs text-muted">Tâches</p><p className="mt-2 text-3xl font-bold">{data.tasks?.length||0}</p><p className="text-xs text-muted">dans votre périmètre</p></Card><Card className="bg-white"><p className="text-xs text-muted">Équipes</p><p className="mt-2 text-3xl font-bold">{data.teams?.length||0}</p><FaUsers className="mt-2 text-primary"/></Card><Card className="bg-white"><p className="text-xs text-muted">Échéance</p><p className="mt-2 text-base font-bold">{data.plannedEndDate?new Date(data.plannedEndDate).toLocaleDateString('fr-FR'):'Non définie'}</p><FaClock className="mt-2 text-amber-500"/></Card></div><div className="mt-6 space-y-5">{data.teams?.map(team=><Card key={team.id} className="bg-white"><div className="flex items-center justify-between gap-3"><div><h2 className="font-bold">{team.name}</h2><p className="text-xs text-muted">Poids projet : {team.weight}%</p></div><FaUsers className="text-primary"/></div><div className="mt-4 space-y-3">{team.sections?.map(section=><div key={section.id} className="rounded-xl border border-line"><button onClick={()=>setOpen(o=>({...o,[section.id]:!o[section.id]}))} className="flex w-full items-center justify-between gap-3 p-4 text-left"><div className="flex items-center gap-3">{open[section.id]?<FaChevronDown/>:<FaChevronRight/>}<div><p className="text-sm font-bold">{section.name}</p><p className="text-xs text-muted">Poids : {section.weight}% · {section.tasks?.length||0} tâches</p></div></div><span className="text-xs font-semibold text-primary">{section.tasks?.filter(t=>['VALIDATED','COMPLETED'].includes(t.status)).length||0}/{section.tasks?.length||0} validées</span></button>{open[section.id]&&<div className="border-t border-line p-4"><div className="space-y-2">{section.tasks?.map(t=><div key={t.id} className="flex flex-col gap-2 rounded-lg bg-surface-2 p-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">{t.title}</p><p className="text-xs text-muted">{t.status} · {t.estimatePoints||1} point(s) · {t.assigneeId||'Non assignée'}</p></div>{t.status==='VALIDATED'?<span className="inline-flex items-center gap-1 text-xs font-semibold text-emerald-600"><FaCheck/> Validée</span>:t.blocked?<span className="inline-flex items-center gap-1 text-xs font-semibold text-red-600"><FaExclamationTriangle/> Bloquée</span>:<span className="text-xs text-muted">{t.deadline?new Date(t.deadline).toLocaleDateString('fr-FR'):'Sans échéance'}</span>}</div>)}</div><div className="mt-4 flex flex-col gap-2 sm:flex-row"><input value={taskTitle} onChange={e=>setTaskTitle(e.target.value)} className="min-w-0 flex-1 rounded-xl border border-line px-3 py-2 text-sm" placeholder="Ajouter une tâche à cette section"/><Button onClick={()=>addTask(section.id)} disabled={saving||!taskTitle.trim()}><FaPlus/> Ajouter</Button></div></div>}</div>)}</div></Card>)}{!data.teams?.length&&<Card className="bg-white py-16 text-center text-sm text-muted">Aucune équipe n'est encore rattachée à ce projet.</Card>}</div><div className="mt-6"><Link to={`/projets/${id}/analytics`} className="text-sm font-semibold text-primary">Voir l'analyse détaillée du projet →</Link></div></div>}
